import { fancyUtil } from "./childcomponentutils";
import "./loginButton.css";

export const LoginButton = () => {
    return (
        <button onClick={fancyUtil} className="btn">
            loginButton
        </button>
    );
};
