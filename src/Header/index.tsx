import React from "react";
import { LoginButton } from "./LoginButton/loginButton";

export const Header = () => {
    return (
        <div>
            Header
            <LoginButton />
        </div>
    );
};
